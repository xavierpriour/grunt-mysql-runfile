# Box definition
box: wercker/nodejs

# Service definition
services:
  - wercker/mysql

# Build definition
build:
  # Build steps
  steps:
    # Runs npm install
    - npm-install

    # Runs grunt
    - grunt
